<%= image_tag @post.image %>
<h1><%= @post.title %></h1>
<h2><%= @post.author %></h2>
<p><%= @post.content %></p>
<%= link_to 'Edit', edit_post_path(@post) %>
<%= link_to 'Delete', @post, method: :delete, data: { confirm: 'Do you wanna delete this post?' } %>
<%= link_to 'Main Page', root_path %>
